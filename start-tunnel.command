#!/usr/bin/env bash
# start-tunnel.command – Doppelklick am Mac: Website syncen + SSH-Tunnel starten.
# Terminal-Fenster dabei offen lassen (Ctrl+C beendet den Tunnel).
cd "$(dirname "$0")"
exec ./run.sh
